import React, { Component } from "react";

class DecisionReveal extends Component {
  render() {
    // console.log(this.props);

    return (
      <div>
        <div className="decision-title">Reveal Page</div>
      </div>
    );
  }
}

export default DecisionReveal;
